# zooba 商談パイプラインレポート

GitHub Pages: https://sawasaka.github.io/zooba-pipeline-report/
リポジトリ: https://github.com/Sawasaka/zooba-pipeline-report

## プロジェクト概要

zooba の商談パイプラインを可視化するCEO向けエグゼクティブレポート。
単一HTMLファイル（`index.html`）で完結。`executive_summary.html` は同一内容のコピー。

## デザイン仕様

- トンマナ: ネイビー(#0D1B2A) × ゴールド(#C9A84C)のエグゼクティブスタイル
- 見出し: Hiragino Mincho ProN（明朝体）
- 本文: Hiragino Sans（ゴシック体）
- KPI数値のカウントアップアニメーション、フェードインアニメーション付き

## ステージ定義

| ステージ | CSSクラス | 色 | KPIラベル |
|---|---|---|---|
| PJ化予定あり | `status-ph3` | gold (#C9A84C) | PJ化予定あり |
| PJ可能案件 | `status-ph4` | blue (#5B8DB8) | PJ可能案件 |
| PJ進行 | `status-ph5` | green (#2ECC71) | PJ進行 |

## ビジネスルール

### 月別帰属

商談は**初回登録月**に帰属する。3月に登録した商談は、4月以降にステージ変更があっても3月タブ内で更新する。新規企業は現在月のタブに追加する。

### KPIウォーターフォール（累積カウント）

KPIカードの数値は「その段階を**通過した累積数**」。上位ステージに移行しても下位の数値は減らさない。**失注・契約も通過扱い**（PJ進行からの派生なので、PJ進行以下の全ステージを通過済みとカウント）。

```
有効商談数 ≥ PJ化予定あり ≥ PJ可能案件 ≥ PJ進行
```

ステージ移行例：1社が「PJ化予定あり」→「PJ進行」に移行した場合、途中の「PJ可能案件」も経由するため：
- PJ化予定あり: 変化なし（すでにカウント済み）
- PJ可能案件: +1
- PJ進行: +1

失注・契約への移行例：PJ進行の1社が失注になった場合：
- PJ進行: **変化なし**（すでに通過済みとしてカウント済み）
- PJ可能案件: 変化なし
- 失注: +1

### 移行率の計算

- 最終転換率 = PJ進行（累積） ÷ 有効商談数
- 各段階の移行率 = 次段階の累積数 ÷ 当該段階の累積数
- 累積数には失注・契約も含む（上記「通過扱い」ルール参照）
- 小数点以下切り捨て、%表示

### ツールチップの企業リスト

各KPIカードのホバーツールチップには、**現在そのステージにいる企業のみ**を表示する（上位に移行済みは除く）。該当なしの場合は「該当なし」と表示。

### タブ間データ同期（重要）

**1社の更新は必ず全ての該当タブに反映する**。商談データは複数タブに重複表示されるため、片方だけ更新するとタブ間で不整合が発生する。

各企業が出現するタブ：
- **全体タブ**：すべての商談（必須反映）
- **月次タブ**（3月／4月／5月など）：登録月のタブ（必須反映）
- **ダイレクトタブ**：パートナー以外の商談（パートナータグなしの企業のみ）
- **パートナータブ**：パートナー経由の商談（パートナータグ付きの企業のみ）

更新時のチェックリスト：
1. ステータスバッジ（status-ph3 / status-ph4 / status-ph5 / status-trial / status-lost / status-dropped）を全該当タブで揃える
2. 備考（deal-note）テキストを全該当タブで同一にする
3. KPIカードの数値・移行率・最終転換率・ツールチップを各タブごとに再計算する
4. outcome（検証／契約／失注／ロスト）カードの累積カウントとツールチップを更新する
5. 全体タブ最下部の「無料トライアル移行率」セクションの内訳・合計も同期する
6. `executive_summary.html` にコピー（内容は `index.html` と完全一致させる）

タブ間の食い違いに気付いた場合は **全体タブを正とする**。

## 更新後の手順

1. `index.html` を更新
2. `executive_summary.html` にコピー
3. git commit & push（GitHub Pagesに自動デプロイ）
